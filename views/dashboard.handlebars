{{! Must display all posts associated with the user}}

{{! Must display all comments associated with the user}}

{{! Posts and comments must link back to the "parent". Post will load its own view. Comments will load the post containing the comment}}

{{! Loads all posts created on the tech bog with active links to view each post in its own page}}
<style>
	h5, h6 { margin: 5px; padding: 5px; display: flex; align-items: center; } h5
	{ text-align: left; font-size: x-large; justify-content: flex-start; } h6
	{text-align: right; font-size: small; justify-content: flex-end;}
	.card-title { border-radius: 10px; }
</style>
<div class="container-fluid">
	{{#each posts as |post| }}
		{{> post this}}
	{{/each}}
</div>
<div class="container-fluid">
	{{#each comments as |comment|}}
		<div class="card comment">
			<div class="card-body">
				<div class="row card-title bg-dark">
					<h5 class="col comment-title font-weight-bold text-light">
						From
						{{comment.post.title}}
					</h5>
					<h6
						class="comment-user comment-time col font-weight-light text-muted"
					>
						Posted by
						{{comment.user.name}}
						on
						{{comment.updatedAt}}
					</h6>
				</div>
				<div class="comment-text">
					<p class="card-text">
						{{comment.content}}
					</p>
					<a href="#" class="card-link">
						View Post
					</a>
				</div>
			</div>
		</div>
	{{/each}}
</div>